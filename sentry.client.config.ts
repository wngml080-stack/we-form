// ============================================
// Sentry Client Config (Legacy)
// Next.js 15+에서는 src/instrumentation-client.ts에서 초기화
// 이 파일은 withSentryConfig 호환성을 위해 유지
// ============================================

// Sentry.init()은 src/instrumentation-client.ts에서 수행됨
// 중복 초기화 방지를 위해 이 파일에서는 init 호출하지 않음
